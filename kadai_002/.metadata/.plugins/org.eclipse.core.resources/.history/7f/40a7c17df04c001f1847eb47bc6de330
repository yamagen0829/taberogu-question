let maxDate = new Date();
 maxDate = maxDate.setMonth(maxDate.getMonth() + 3);
 
 flatpickr('#fromReservedDatetime', {
   mode: "single",
   locale: 'ja',
   minDate: 'tomorrow',
   maxDate: maxDate
 });